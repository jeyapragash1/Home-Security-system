# Sentinel Safe - Apache Configuration
# Production-ready .htaccess with security best practices

# Enable RewriteEngine
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Force HTTPS (uncomment in production)
    # RewriteCond %{HTTPS} off
    # RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
    
    # Remove www (optional - uncomment if desired)
    # RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
    # RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
</IfModule>

# ============================================
# SECURITY HEADERS
# ============================================

<IfModule mod_headers.c>
    # Prevent clickjacking attacks
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Prevent MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Enable XSS protection
    Header always set X-XSS-Protection "1; mode=block"
    
    # Referrer policy
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Content Security Policy (adjust as needed)
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://unpkg.com; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com https://unpkg.com https://cdnjs.cloudflare.com https://stackpath.bootstrapcdn.com; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data:; connect-src 'self';"
    
    # Permissions Policy (formerly Feature Policy)
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=()"
    
    # HSTS - HTTP Strict Transport Security (uncomment in production with HTTPS)
    # Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    
    # Remove server signature
    Header unset Server
    Header always unset X-Powered-By
</IfModule>

# ============================================
# PROTECT SENSITIVE FILES
# ============================================

# Protect .env file
<FilesMatch "^\.env">
    Require all denied
</FilesMatch>

# Protect log files
<FilesMatch "\.log$">
    Require all denied
</FilesMatch>

# Protect SQL files
<FilesMatch "\.sql$">
    Require all denied
</FilesMatch>

# Protect configuration files
<FilesMatch "^(config\.php|Security\.php|Logger\.php|Validator\.php)$">
    Require all denied
</FilesMatch>

# Protect backup files
<FilesMatch "\.(bak|backup|swp|old|orig|save)$">
    Require all denied
</FilesMatch>

# Protect Git files
<FilesMatch "^\.git">
    Require all denied
</FilesMatch>

# ============================================
# DIRECTORY PROTECTION
# ============================================

# Prevent directory listing
Options -Indexes

# Protect logs directory
<IfModule mod_rewrite.c>
    RewriteCond %{REQUEST_URI} ^/logs/
    RewriteRule ^.*$ - [F,L]
</IfModule>

# Protect config directory
<IfModule mod_rewrite.c>
    RewriteCond %{REQUEST_URI} ^/config/
    RewriteCond %{REQUEST_URI} !\.css$ [NC]
    RewriteCond %{REQUEST_URI} !\.js$ [NC]
    RewriteRule ^.*$ - [F,L]
</IfModule>

# ============================================
# PHP CONFIGURATION
# ============================================

<IfModule mod_php7.c>
    # Hide PHP version
    php_flag display_errors Off
    php_value error_reporting 32767
    php_flag log_errors On
    php_value error_log logs/php-errors.log
    
    # Session security
    php_value session.cookie_httponly 1
    php_value session.use_only_cookies 1
    php_value session.cookie_samesite Strict
    
    # File upload limits
    php_value upload_max_filesize 5M
    php_value post_max_size 6M
    php_value max_execution_time 30
    php_value max_input_time 30
    php_value memory_limit 128M
    
    # Disable dangerous functions
    php_value disable_functions "exec,passthru,shell_exec,system,proc_open,popen,curl_exec,curl_multi_exec,parse_ini_file,show_source"
</IfModule>

# ============================================
# PERFORMANCE OPTIMIZATION
# ============================================

# Enable GZIP Compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/x-javascript application/json
</IfModule>

# Browser Caching
<IfModule mod_expires.c>
    ExpiresActive On
    
    # Images
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # CSS and JavaScript
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    
    # Fonts
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType application/font-woff2 "access plus 1 year"
    
    # HTML (shorter cache)
    ExpiresByType text/html "access plus 1 hour"
    
    # Default
    ExpiresDefault "access plus 1 month"
</IfModule>

# ============================================
# ERROR DOCUMENTS
# ============================================

# Custom error pages (create these files if needed)
# ErrorDocument 400 /errors/400.html
# ErrorDocument 401 /errors/401.html
# ErrorDocument 403 /errors/403.html
# ErrorDocument 404 /errors/404.html
# ErrorDocument 500 /errors/500.html
# ErrorDocument 503 /errors/503.html

# ============================================
# CHARSET
# ============================================

# Default charset
AddDefaultCharset UTF-8
<IfModule mod_mime.c>
    AddCharset UTF-8 .html .css .js .xml .json .rss .atom
</IfModule>

# ============================================
# MIME TYPES
# ============================================

<IfModule mod_mime.c>
    # JavaScript
    AddType application/javascript js
    AddType text/javascript js
    
    # JSON
    AddType application/json json
    
    # Images
    AddType image/svg+xml svg svgz
    AddType image/webp webp
    
    # Fonts
    AddType application/font-woff woff
    AddType application/font-woff2 woff2
    AddType application/vnd.ms-fontobject eot
    AddType font/ttf ttf
    AddType font/otf otf
</IfModule>

# ============================================
# HOTLINK PROTECTION (Optional)
# ============================================

# Uncomment to prevent hotlinking of images
# <IfModule mod_rewrite.c>
#     RewriteCond %{HTTP_REFERER} !^$
#     RewriteCond %{HTTP_REFERER} !^https?://(www\.)?yourdomain\.com [NC]
#     RewriteRule \.(jpg|jpeg|png|gif|svg)$ - [F,L]
# </IfModule>

# ============================================
# BLOCK BAD BOTS (Optional)
# ============================================

# <IfModule mod_rewrite.c>
#     RewriteCond %{HTTP_USER_AGENT} ^.*(bot|crawler|spider|scraper).*$ [NC]
#     RewriteRule .* - [F,L]
# </IfModule>

# ============================================
# IP BLOCKING (Optional)
# ============================================

# Block specific IP addresses
# <RequireAll>
#     Require all granted
#     Require not ip 123.456.789.0
#     Require not ip 98.765.43.21
# </RequireAll>

# ============================================
# ADMIN AREA PROTECTION (Optional)
# ============================================

# Restrict admin areas to specific IPs
# <FilesMatch "^(settings|admin)\.php$">
#     <RequireAny>
#         Require ip 192.168.1.0/24
#         Require ip 10.0.0.0/8
#     </RequireAny>
# </FilesMatch>

# ============================================
# END OF CONFIGURATION
# ============================================

# Note: Restart Apache after making changes
# sudo service apache2 restart
# or
# sudo systemctl restart httpd
